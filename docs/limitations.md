# Limitations

This is a linear binary classifier, not a deep neural network. No hidden layers, automatic differentiation, regularization, or feature scaling are included. Pure Python loops target small experiments. The threshold matters, and accuracy alone is insufficient for imbalanced data.
